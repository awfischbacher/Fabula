namespace Fabula.Models
{
    // Represents the type of post in the board, allowing users to filter it.
    public enum PostType
    {
        ANNOUNCEMENT,
        DISCUSSION,
        DEBATE,
        HELP
    }
}
